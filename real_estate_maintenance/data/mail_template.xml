<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">

        <template id="mail_notification_sign_online" inherit_id="mail.mail_notification_paynow"
            name="Delegation: Sign mail notification template">
            <xpath expr="//t[@t-set='access_name']" position="after">
                <t t-if="record._name == 'real.estate.delegate'">
                    
                    <t t-if="record.has_to_be_signed(include_draft=True)">
                        
                        <t t-set="access_name">Accept &amp; Sign </t>
                    </t>
                    
                    
                </t>
            </xpath>
        </template>

    <record id="email_template_delegation" model="mail.template">
        <field name="name">Delegation: Send by email</field>
        <field name="model_id" ref="real_estate_maintenance.model_real_estate_delegate"/>
        <field name="subject"> Delegation Order Ref ${object.name or 'n/a' }</field>
        <field name="email_from">${(object.user_id.email_formatted or user.email_formatted)}</field>
        <field name="email_to">${object.partner_id.email}</field>
        <field name="body_html" type="html">
            <div style="margin: 0px; padding: 0px;">
                <p style="box-sizing:border-box;margin: 0px; padding: 0px; font-size: 13px;">

                Hello ${object.partner_id.name},
                <br/><br/>
                Your delegation  <strong style="box-sizing:border-box;font-weight:500;">${(object.name or '').replace('/','-')}</strong>
                amounting in  <strong contenteditable="false" oe-keep-contenteditable=""> ${format_amount(object.total_amount, object.currency_id)} .
                </strong> is ready for review.
                <br/><br/>
                Do not hesitate to contact us if you have any questions.
                <br/>

                </p>
            </div>
        </field>
        <!--field name="report_template" ref="action_report_saleorder"/-->
        <field name="report_name">${(object.name or '').replace('/','_')}</field>
        <field name="lang">${object.partner_id.lang}</field>
        <field name="auto_delete" eval="True"/>
    </record>


    </data>

</odoo>